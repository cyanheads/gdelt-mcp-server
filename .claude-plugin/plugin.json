{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "gdelt-mcp-server",
  "version": "0.4.3",
  "description": "Search and analyze global news coverage and US TV transcripts via the GDELT Project APIs.",
  "author": {
    "name": "cyanheads",
    "email": "casey@caseyjhand.com",
    "url": "https://github.com/cyanheads"
  },
  "homepage": "https://github.com/cyanheads/gdelt-mcp-server#readme",
  "repository": "https://github.com/cyanheads/gdelt-mcp-server",
  "license": "Apache-2.0",
  "keywords": [
    "mcp",
    "mcp-server",
    "model-context-protocol",
    "gdelt",
    "global-news",
    "news-analytics",
    "media-monitoring"
  ],
  "userConfig": {
    "gdelt_base_url": {
      "type": "string",
      "title": "GDELT Base URL",
      "description": "Override the base URL for the GDELT DOC and TV APIs.",
      "required": false,
      "default": ""
    },
    "gdelt_request_delay_ms": {
      "type": "number",
      "title": "GDELT Request Delay (ms)",
      "description": "Minimum milliseconds between GDELT requests, enforcing the 1 req/5s rate limit.",
      "required": false,
      "default": 5300
    },
    "gdelt_request_timeout_ms": {
      "type": "number",
      "title": "GDELT Request Timeout (ms)",
      "description": "Deadline in milliseconds for a single GDELT request; the whole call, retries included, is bounded at twice this value.",
      "required": false,
      "default": 60000
    }
  },
  "mcpServers": {
    "gdelt-mcp-server": {
      "command": "npx",
      "args": ["-y", "@cyanheads/gdelt-mcp-server"],
      "env": {
        "MCP_TRANSPORT_TYPE": "stdio",
        "GDELT_BASE_URL": "${user_config.gdelt_base_url}",
        "GDELT_REQUEST_DELAY_MS": "${user_config.gdelt_request_delay_ms}",
        "GDELT_REQUEST_TIMEOUT_MS": "${user_config.gdelt_request_timeout_ms}"
      }
    }
  }
}
